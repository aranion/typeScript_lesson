{
  "name": "lesson",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf public/scripts && tsc",
    "build:dev": "rimraf public/scripts && tsc -p tsconfig.dev.json",
    "start": "http-server -p 8080 public",
    "start:json": "json-server --watch ./db.json",
    "lint": "eslint src --ext .js --ext .ts",
    "lint-fix": "eslint src --ext .js --ext .ts --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aranion/typeScript_lesson.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/aranion/typeScript_lesson/issues"
  },
  "homepage": "https://github.com/aranion/typeScript_lesson#readme",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "eslint": "^8.1.0",
    "http-server": "^14.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
